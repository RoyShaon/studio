/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here.
  // For now, we'll keep it simple to ensure the build passes.
};

export default nextConfig;
